spring:
# DataSource
  datasource:
    url: jdbc:mysql://localhost/booksDB?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
# JPA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
# Thymeleaf
  thymeleaf:
    cache: false